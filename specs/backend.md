# Backend

The backend is a Python app that uses FastAPI to serve the frontend and manage the game state.

## Frontend

The backend will parse the game configs and provide the frontend with the necessary data to display the game.

## Game State

The game state is stored in the backend and is used to keep track of the current state of the game. The game state is updated in real-time and is sent to the frontend as the game progresses.
